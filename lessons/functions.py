"""Practice with functions."""


# (comment: ex of define a function)
def sum(num1: int, num2: int) -> int:
    """Return num1 + num2."
    return num1 + num2


# Call the function
print(sum(num1 = 1, num2 = 10)) # <- 1 and 10 are arguments
# calling a randint sum(num1 = randint(1,10), num2 = radiant(2,20))
# parameter are to define a function, argument is to call a functions